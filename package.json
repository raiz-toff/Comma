{
  "name": "macadam",
  "version": "1.0.0",
  "private": true,
  "description": "Macadam — Local-first gig driver earnings app",
  "scripts": {
    "dev": "node build.js --dev",
    "build": "node build.js --prod",
    "rebuild:provinces": "node scripts/rebuild-province-index.js",
    "preview": "npx --yes serve dist -s -l 3000"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
