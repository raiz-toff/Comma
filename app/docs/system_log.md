2026-06-28T22:27:01Z | Remove persona system — replaced with operationalModelId across useSettingsStore, useAppContext, OnboardingWizard, settings screen, dashboard, and reportGenerator; deleted personas.ts and usePersonaVocabulary.ts
2026-06-28T23:57:37Z | Gamification reorganization: split goals/index.tsx into Goals+Progress tabs; audited badge registry (removed rain_rider/peak_collector/multi_app_master/bonus_hunter/data_archivist, added first_expense/mileage_master/road_warrior/platform_expert/daily_grinder); added weekly challenge reset with nextResetDate/weekStartedAt; streak card with shields tooltip and milestone ring.
2026-06-29T00:01:30Z — Reports page UI revamped: added period pill + arrow nav + bar chart + 2x2 stat grid + period selector modal with presets, month list with mini bars, and custom date range picker; added getMonthlyStatsForYear query to analytics.ts
2026-06-29T00:27:30Z [tax-ui-quietdown] Restructured tax tab to 3-card layout (Tax Jar, Obligations total, Next Installment row); moved obligations breakdown, CPP1/2, UK NI, state tax rows, quarterly projection to Tax Center detail screen; settings moved to bottom sheet modal
2026-06-29T00:40:41Z | tax tab header cleanup: removed platform switcher and bell from GlobalTopHeader on /tax route, moved Tax title + gear into hamburger row space via paddingLeft:70 alignment
2026-06-29T00:49:04Z | tax/center.tsx: total UI rework to match StyleSheet design system from tax/index.tsx; fixed jar adjust snappiness (local state + correct queryKey for optimistic update)
2026-06-29T01:33:41Z | Redesigned onboarding: removed persona/WorkType step and BranchStep; added universal PlatformStep, AppearanceStep (theme + accent colour), and PreferencesStep (week start, time format, optional feature toggles); new 9-step flow: Name → Country → Region → Platforms → Vehicle → Goal → Appearance → Preferences → GPS → Reveal; added accentColor to DriverProfile.
- Reworked CSV Import Wizard (Task 4.3): restyled to match app design system (black/#0d0d0d/#1f1f1f cards, platform-accent theme via usePlatformTheme, no emoji/slate-emerald). Hardened validation: flexible date parser (ISO/epoch/M-D-Y/D-M-Y + 12/24h), registry-based platform matching across all PLATFORM_REGISTRY ids/aliases, end>=start check, negative clamping, per-reason skip reporting, and a scrollable native column-picker modal (replaces Alert button cap). Imports via insertManyShifts and invalidates ['shifts']+['analytics'].
- [2026-06-29] Reports: replaced native Alert.alert() in CSV/PDF export with themed FeedbackDialog + BusyOverlay (src/components/ui/FeedbackDialog.tsx). Hardened export handlers: empty-data guard ("Nothing to Export"), Sharing.isAvailableAsync() check, web pop-up-blocked detection, double-tap re-entry guard via busyExport state, per-row spinners, and success confirmations.
2026-06-29T01:55:45Z | TimePickerModal (src/components/ui/TimePickerModal.tsx): fixed wheel scroll/selection — reliable initial position via onContentSizeChange (replaced racey setTimeout), live highlight via throttled onScroll, selection bar re-anchored to the wheel row (removed magic +44 offset), per-open remount to reset to incoming value, nestedScrollEnabled for Android Modal/Pressable nesting.
- [2026-06-29] Settings: fixed CSV export doing nothing on Android — root cause was RN Share.share({url}), which Android silently ignores. Switched to expo-sharing (Sharing.shareAsync) like the Reports screen. Replaced the generic Alert.alert("Which data?") chooser with a themed FeedbackDialog (stacked Shifts/Expenses actions). Added empty-data guard, isAvailableAsync check, BusyOverlay, and success/error feedback. Extended FeedbackDialog with an `actions` prop for stacked choice buttons.
2026-06-29T02:27:05Z | Notifications overhaul: added badgeId/iconKey to NotificationItem so badge unlocks render their actual BadgeSvg in the panel; added centralized addNotification store action with a serialization lock (runGamificationStateMutation) to prevent clobbering shared gamification state; created src/services/notify.ts helper + presets (notifyBackup/Restore/Export/Wipe/Import); redesigned app/notifications.tsx with Today/Yesterday/Earlier grouping, 40px icon container, dedicated dismiss column, and clean action bar; wired operational outcomes into the panel (Drive backup/restore, reports CSV/PDF export, tax export, platform data wipe, CSV import); added unread-count badge to the GlobalTopHeader bell.
2026-06-29T02:36:42Z | Google Drive backup (Phase 12.3) — cross-device rework: encryption key now derived from the backup password ALONE (PBKDF2 210k iters + per-file salt stored in the .comdb envelope) instead of a random per-device key, so backups restore on a new device. New encryptBackup/decryptBackup replace getOrCreateEncryptionKey/encrypt/decrypt in cryptoHelper(.native).ts. backupToDrive now covers all 11 user-data tables (was 5; added maintenanceLogs, taxHistory, platforms, shiftPlatforms, merchants, vehicleTaxProfiles; excludes bulky GPS locationPoints/tempNativePoints). restoreFromDrive is transactional, version-aware (v2), only wipes tables present in the backup, and revives Date columns before insert. Settings Data tab: 4-digit PIN replaced by a min-6 backup password with cross-device/loss warning. Pending: wiring the real OAuth Web client ID.
2026-06-29T02:37:42Z | Consolidated notifications to one screen (Option A): removed the duplicate hardcoded slide-out notifications panel and its mock INITIAL_NOTIFICATIONS from app/(tabs)/_layout.tsx (state, refs, animations, JSX, ~20 orphan styles, unused imports). The header bell now always navigates to the real store-backed app/notifications.tsx everywhere, so badge SVGs, operational notifications, and the unread count work from the main tabs.
2026-06-29T02:44:34Z | Restyled app/notifications.tsx to match the app's warm-slate design language: replaced cool NativeWind slate-* classes with inline styles using the shared palette (#000 bg, #0d0d0d cards w/ 0.8px #1f1f1f borders, #161615 icon boxes w/ #262522 borders, circular 44px header back button, accentColor from usePlatformTheme for active/unread states, warm muted text #a1998f/#52504c). Now visually consistent with goals/reports/settings screens.
2026-06-29T02:50:26Z | Google Drive backup: wired the Web OAuth client via new src/config/google.ts (GOOGLE_WEB_CLIENT_ID 438513486290-hvsmc...). GoogleSignin.configure now passes webClientId+scopes+offlineAccess; expo-auth-session clientId and refreshGoogleToken use it too (no YOUR_CLIENT_ID placeholders left). Replaced the FormData+Blob Drive upload with a manual multipart/related string body (React Native does not serialize Blob FormData parts reliably), added timestamped filenames and HTTP-status error detail. Android OAuth client created (pkg com.anonymous.comma, debug SHA-1 CE:D3:8A...). Remaining before it runs: enable Google Drive API + add a test user in the Cloud console, then build a dev client (expo run:android) to test backup/restore.
2026-06-29T03:16:12Z | Google Drive backup UX: replaced the cramped Settings>Data inline card with a guided hub screen app/settings/backup.tsx (route /settings/backup). Three progressive steps (Connect Google Drive -> Set backup password -> Back up now) that unlock in order, plus a "Your backups" list with confirm-then-restore. New src/services/backupPassword.ts persists the password in SecureStore so repeat backups dont re-prompt; restore asks for it only when not stored locally (new-device case). Added userEmail to useGoogleDriveSync (shows connected account). Reused FeedbackDialog/BusyOverlay; custom PasswordPrompt modal for set/enter. Settings Data tab now just a single "Google Drive Backup -> Open" row. tsc clean.
2026-06-29T05:46:48Z fix(background): add battery opt exemption, boot receiver, and exact-alarm restart to keep LocationTrackingService alive after swipe-away on OEM devices
2026-06-29T07:47:39Z | pre-release audit fixes: resolved all 5 critical + 21 high + 19/24 medium + ~18/22 low findings across native FGS (Android 14 startForeground type, background-start crash guards, dropped SCHEDULE_EXACT_ALARM, BootReceiver staleness, notification content intent, PII log scrub), GPS permission flow (no bg hard-gate, canAskAgain/services-off, native permission guard+Boolean return), money math (deductiblePct in tax/net-income/overview/monthly/tax-center, localtime SQL bucketing), Rules-of-Hooks in analytics, AppErrorBoundary + global error handler, Drive resilience (token refresh/single-flight/timeouts/atomic web restore/await-invalidate/surfaced list errors), transactional shift+CSV saves, SQL pagination filter, app id app.comma.tracker + perm-removal config plugin + notif icon/color + cleartext/allowBackup off, plus perf/a11y/dead-code cleanups. tsc: 15 pre-existing errors, 0 introduced.
2026-06-29T08:30:00Z | Design system overhaul (Uber/DoorDash-informed): rewrote token sources to a cool-neutral cool ramp — src/global.css .dark block (Surface/01-05 #0A0A0C..#26262C, Text primary/secondary/muted/disabled, Border subtle/strong, fixed semantics Success #1FC16B / Warning #F5A623 / Danger #FF5247 / Info #3B82F6) + tailwind.config.js (new surface/content/line color tokens, radius sm8/md12/lg16/xl20/2xl28, Base-style fontSize ramp display..paragraph-s with 2xs..5xl kept as aliases). Extended src/components/ui/text.tsx to the ramp variants + a `tabular` prop (fontVariant tabular-nums); CurrencyText now tabular + semantic money colors. Restyled all 13 shared ui/ components + 9 analytics widgets + CSVImportWizard/ErrorBoundary off the warm palette. Wired BottomSheetModalProvider in app/_layout.tsx and added src/components/ui/AppBottomSheet.tsx (radius-2xl, Surface/03, grabber, backdrop, safe-area); converted the Dashboard Start Shift wizard from a centered Modal to AppBottomSheet. Dashboard hero now Display-size tabular net; stat row tabular + uppercase labels. Onboarding progress rail now borrows the chosen accent (theming starts at setup), dropped the numeric counter. Built Bulletin Mode: src/components/celebration/CelebrationSheet.tsx (badge medallion, Display headline, Reanimated confetti, Share/Done) triggered from handleEndShift via evaluateGamification() now returning newly-unlocked badge IDs; flipped gamification feature default ON in useAppContext. Rethemed all 24 screens — zero warm-drift hexes remain app-wide. tsc: 15 pre-existing baseline errors, 0 introduced.
2026-06-29T09:10:00Z | Backup reminder (was dormant): wired up the previously-inert "Backup overdue" toggle + last_backup_at timestamp into an actual nudge. New hooks/useBackupStatus.ts reads last_backup_at (settings KV / comma_last_backup_at on web) + notification_prefs.backupOverdue, computes daysSince, and returns isOverdue when the reminder is enabled AND it's been >=7 days (BACKUP_STALE_DAYS) or never. Reminder now defaults ON (opt-out) for this local-only app — both the hook (treats missing pref as enabled, only an explicit false disables) and the Settings→Alerts default flipped to true. Added an Info-blue dismissible Dashboard banner under the unreconciled-shifts banner: shows "It's been N days since your last backup" (or a never-backed-up invite), deep-links to /settings/backup, gated on recentShifts.length>0 so fresh installs aren't nagged, session-dismissible via ✕. Backup success in backup.tsx now invalidates ["backup","status"] so the banner clears immediately. Also ported the authentic Google Drive logo 1:1 (src/components/logos/GoogleDriveLogo.tsx, mask + 3 gradient faces, rounded corners preserved) onto the backup intro card. tsc: 15 pre-existing baseline errors, 0 introduced.
2026-06-30 — cloud-sync P1: migrated src/database/queries/goals.ts to synced writes/reads (stampInsert on insertGoal, soft-delete on deleteGoal, tombstone filters on getGoalsWithProgress goals + shift sub-reads).
- 2026-06-30 — cloud-sync P1: migrated maintenance.ts and taxProfiles.ts query files to sync semantics (stampInsert/stampUpdate on writes, soft-delete tombstones, notDeleted/isNotDeleted read filtering, upsertTaxProfile revives soft-deleted rows).
- 2026-06-30 — cloud-sync P1: migrated src/database/queries/expenses.ts — stamped insert/update (expenses + auto-created merchants), converted deleteExpense to soft-delete tombstone, added notDeleted/isNotDeleted read filters to all expense SELECTs incl. critical aggregations (getExpenseYTDSummary, getExpensesByTaxCode, getExpensesByMonth) and getRecentMerchants (merchants + expenses).
- 2026-06-30 — cloud-sync P1: migrated src/database/queries/tax.ts and platforms.ts to sync semantics. tax.ts: stampInsert on insertTaxHistory (native + web), notDeleted/isNotDeleted on getTaxHistory, tombstone filters on getTaxYearSummary's shifts + expenses aggregations; getTaxJarBalance/setTaxJarBalance left unchanged (settings KV is not synced). platforms.ts: notDeleted/isNotDeleted on getDBPlatforms, stampInsert/stampUpdate on updateDBPlatform (both insert + update paths, both branches), seedDBPlatforms now reads raw rows to revive tombstoned defaults in place (clear syncDeletedAt + stampUpdate) and stampInsert on fresh inserts.
- 2026-06-30 — cloud-sync P1 COMPLETE: schema adds syncUpdatedAt/syncDeletedAt to all 10 synced record tables (settings/locationPoints/tempNativePoints excluded), migration 0017_tough_lester generated + bundled; new src/database/syncedWrites.ts (stampInsert/stampUpdate/softDeletePatch/notDeleted/isNotDeleted) and src/database/syncState.ts (sync KV keys + Reset helpers); all query files (vehicles, goals, maintenance, taxProfiles, expenses, shifts, tax, platforms, analytics) migrated to soft-delete + stamped writes + tombstone-filtered reads; Reset App (useSettingsStore.resetSettings) rewritten to local-only hard wipe + sync_enabled=0 + re-minted device_id + cleared cursors (cloud untouched, paid status survives via RevenueCat). Also fixed 12 pre-existing analytics.ts type errors (getPlatformConditions SQL|undefined). Typecheck: 15→3 errors, the 3 remaining are pre-existing unrelated gamification Challenge-type errors.
- 2026-06-30 — cloud-sync P2 COMPLETE (change-log read/write on Drive, manual Sync-now only; merge is a P2 stub pending P3): new src/database/syncedTables.ts (single source of truth for the 10 synced record tables + TIMESTAMP_FIELDS/reviveTimestamps, settings excluded); extended src/database/syncState.ts with cursor helpers (getDeviceId/isSyncEnabled/getLastPushedAt/setLastPushedAt/getAppliedLogs/addAppliedLog, web+native); new src/services/sync/ module: changeLog.ts (ChangeLog type, build/parse filename comma-cl-{deviceId}-{ms}.cmlog, encode/decode via existing encryptBackup), pushChanges.ts (collect rows where syncUpdatedAt>lastPushedAt incl tombstones, encrypt, multipart upload to appDataFolder, advance cursor + applied-set), pullChanges.ts (list .cmlog, filter not-in-applied-set AND not-mine, download+decrypt, return oldest→newest), applyChangeLog.ts (NAIVE UPSERT STUB — flagged, real LWW merge is P3), syncNow.ts (single-flight orchestrator pull→apply→push, sync_enabled gated); new hooks/useSyncNow.ts (manual trigger, invalidates RQ on applied rows). Exported fetchWithTimeout from googleDrive.ts for reuse. Typecheck: 3 errors (all pre-existing gamification Challenge, 0 from P2). NOT tested e2e — needs OAuth console prereqs + physical device build.
- 2026-06-30 — cloud-sync P2 hardening + dev test UI: FIXED a latent bug where nanoid's default alphabet (contains '-') could put a dash in sync_device_id, breaking the filename round-trip and the "not authored by me" pull filter — device ids are now dash-free by construction (customAlphabet) and buildChangeLogFilename no longer sanitizes. Added setSyncEnabled to syncState.ts. Verified pure logic with a standalone tsx script (scratchpad/verify-p2.ts): 16/16 — dash-free invariant, filename build↔parse round-trip, parser rejects .comdb/manifest/garbage, ChangeLog encode↔decode through real AES-GCM/PBKDF2 (tombstones intact), wrong-password/garbage/future-version all throw. Wired a temporary "SYNC · EXPERIMENTAL" section into app/settings/backup.tsx: a sync-enable Switch (stands in for the RevenueCat entitlement until P6) + "Sync now" button (useSyncNow), gated on Drive-connected + password-set + enabled. Typecheck: 3 errors, all pre-existing gamification Challenge, 0 from this work. Still NOT tested e2e (needs OAuth console prereqs + physical device).
- 2026-06-30 — cloud-sync P3 COMPLETE (real merge engine, replaces the P2 stub): new sync_overwrite_log table (local audit, no sync cols, migration 0018_mean_black_cat) + added to Reset wipe; new src/services/sync/mergeRules.ts (pure, testable: decideMerge LWW insert/overwrite/skip with strict-> ties-keep-local, FINANCIAL_TABLES={expenses,taxHistory,shifts,shiftPlatforms}, shouldAuditOverwrite predicate); rewrote applyChangeLog.ts as real read-compare-write LWW in one transaction (native) / localStorage (web) — incoming wins only if strictly newer, tombstones land, and a financial overwrite of a row with prior local edits records the superseded version to sync_overwrite_log BEFORE overwriting (never silently drop money edits); syncNow.ts now reports appliedRows/skippedRows/auditedRows and records each log in the applied-set after its merge tx commits; dev UI dialog shows the richer counts. Verified pure logic via tsx script: 28/28 (12 new P3 cases — LWW matrix + audit gating). Typecheck: 3 errors, all pre-existing gamification Challenge, 0 from P3. Engine now safe to merge two devices; still NOT tested e2e (needs OAuth console + device). NEXT: P4 auto-triggers, P5 compaction, P6 RevenueCat gate.
- 2026-06-30 — cloud-sync P4 COMPLETE (automatic triggers, no setInterval): new src/services/sync/schedule.ts (pure SyncSchedule 'manual'|'daily'|'weekly', SCHEDULE_INTERVAL_MS, isSyncDue, coerceSchedule); syncState.ts gains schedule + lastPushRunAt (wall-clock) KV helpers (+ reset clears them); syncNow.ts parameterized with {pull,push} opts and SERIALIZED via a promise queue (replaced the bare single-flight so a manual full-sync isn't swallowed by an in-flight pull-only), stamps lastPushRunAt on each push; new hooks/useAutoSync.ts mounts an AppState listener in ShiftBackgroundServices (app/_layout.tsx) — FOREGROUND (bg→active)=pull + push-if-due, BACKGROUND (active→bg)=push-if-due; gated on sync_enabled && Drive-connected && backup-password (silent no-op otherwise, errors swallowed). backup.tsx dev section gains a Manual/Daily/Weekly schedule picker. RECONCILIATION (doc §4): user's WhatsApp-style schedule = the auto-PUSH cadence; PULL always on open; manual ignores schedule. Wi-Fi-only deferred (no netinfo dep). Verified pure logic 39/39 via tsx (11 new P4 isSyncDue/coerce cases). Typecheck: 3 errors, all pre-existing Challenge, 0 from P4. Engine P1–P4 done; still NOT tested e2e (OAuth console + device). NEXT: P5 compaction, P6 RevenueCat gate.
- 2026-06-30 — fix: InlineInput/FullInput text visibility — changed hardcoded color:"#F6F6F7" to DS.textPrimary in app/settings/index.tsx inlineInput/fullInput StyleSheet to match working pattern in vehicles/[id].tsx; added selectionColor={DS.textPrimary} to both TextInput wrapper functions for consistent text selection highlight.
- 2026-06-30 — enhance: InlineInput/FullInput text visibility — added cursorColor={DS.textPrimary} to both TextInput components so typed text (e.g., "20" in hourly rate field) is visible; cursor, selection, and typed text all now use DS.textPrimary (#F6F6F7).
- 2026-06-30 — cloud-sync P5 COMPLETE (lazy compaction, no manifest): changeLog.ts adds buildSnapshotFilename + generic parseSyncFilename (kind 'log'|'snapshot'); new src/services/sync/driveIO.ts (shared listAppDataFiles + uploadSyncFile, refactored push/pull to use it); deleteDriveFile added to googleDrive.ts; new compaction.ts maybeCompact() — when >=30 delta .cmlog files, builds a full-state snapshot (.cmsnap), uploads FIRST, then deletes ONLY delta logs in the applied-set (provably subsumed) + own older snapshots; wired best-effort into syncNow after push. pullChanges now merges .cmlog AND .cmsnap. Safety invariant honored (never delete a log until its data is in a snapshot; tolerate leftovers). Verified 44/44 via tsx.
- 2026-06-30 — cloud-sync P6 COMPLETE at the code seam (RevenueCat gate, SDK not yet installed): src/config/revenuecat.ts (SYNC_ENTITLEMENT_ID='sync' + empty public key placeholders + isRevenueCatConfigured); src/services/entitlement.ts soft-requires react-native-purchases (like google-signin pattern) — configureEntitlements/refreshSyncEntitlement/restorePurchases; refreshSyncEntitlement drives sync_enabled from the 'sync' entitlement when configured, else NO-OP so the manual dev toggle stays in control; wired refresh into useAutoSync on app foreground. Typecheck: 3 errors, all pre-existing Challenge, 0 from P5/P6. SYNC ENGINE P1–P6 CODE COMPLETE. Remaining for the USER: OAuth test-user + signing SHA-1 (Drive API already enabled), and for paid sync: npx expo install react-native-purchases + RevenueCat keys + a 'sync' entitlement + store products. Still NOT tested e2e (needs device build).
- 2026-06-30 — fix: InlineInput/FullInput typed text color — added inline style { color: DS.textPrimary } as final element in style array to force text color on typed input; prevents React Native from defaulting typed text to dark color on dark backgrounds (hourly rate, mileage rate, etc now show white text when entered).
- 2026-06-30 — FINAL FIX: InlineInput number visibility — when user toggles "Hourly Rate" ON and enters a number (e.g., 20), the typed number now displays in white (#FFFFFF) instead of dark; applied color: "#FFFFFF" to both s.inlineInput base style AND inline style prop in InlineInput() component to ensure React Native doesn't override with dark default.
- 2026-06-30 — redesign: About screen (app/about/index.tsx) — playful & engaging theme with spring hero animation, 3-column highlights grid (Local/Fast/OpenSource), dedicated mission statement card, improved visual hierarchy, accent-colored icons, better typography, emoji hero icon in rounded accent-border box, icons for OSS packages, streamlined sections with "Privacy First" / "Get Involved" / "Built On" labels.
- 2026-06-30 — refine: About screen personalization — removed technical acknowledgments section, replaced with creator spotlight (Rajkumar Neupane), added portfolio link (rajkumarneupane.com) and "Buy Me a Coffee" link (buymeacoffee.com/raiztuffy) in Connect & Support section, creator card with accent-green background and description of Comma's mission.
- 2026-06-30 — PRODUCT PIVOT: Sync is now the ONLY cloud feature (manual whole-DB backup UI removed), positioned as a single PAID feature "Cloud Sync" (free tier = local only). app/settings/backup.tsx rewritten (BackupScreen→SyncScreen): Connect Drive → set Sync password → auto-enables (the "connect then done" flow; removed the manual Back-up button, the Your-Backups restore list, and the "experimental" framing); schedule picker + Sync now retained. Auto-enable effect sets sync_enabled once Drive+password ready (RevenueCat entitlement governs once configured). Settings→Data entry relabeled "Cloud Backup/Google Drive Backup"→"Cloud Sync"; notification pref "Backup overdue"→"Sync reminder" (key unchanged); Dashboard reminder reworded backup→Cloud Sync. syncNow now writes last_backup_at (via new setLastCloudSaveAt) on each successful push so the "data is safe" status/reminder stays meaningful. Old backupToDrive/restoreFromDrive/listBackups + useGoogleDriveSync backup methods left dormant (auth parts still used). Typecheck: 3 pre-existing Challenge errors, 0 new. Route kept as /settings/backup (file still backup.tsx) to avoid breaking links.
- 2026-06-30 — redesign: About screen — clean minimal rebuild: giant COMMA hero with spring animation, creator byline (Rajkumar Neupane), 4-item link list (portfolio, buy me a coffee, share, diagnostics), removed all filler sections (mission card, privacy cards, highlights grid, acknowledgments). Open-source decision reflected in copy.
- 2026-06-30 — PAYWALL REMOVED, app fully open source / Cloud Sync FREE for everyone (user request, no revenue). DELETED src/services/entitlement.ts + src/config/revenuecat.ts; removed the refreshSyncEntitlement import + foreground call from hooks/useAutoSync.ts. sync_enabled is now just "user turned Cloud Sync on" (auto-enabled when Drive+password ready, cleared on disconnect) — no entitlement governs it. UI copy: header "Premium · encrypted" → "Free · encrypted"; settings hint "...Premium." → "...Free."; comments in syncState.ts / useSettingsStore.ts / backup.tsx de-RevenueCat'd. Design doc §7 marked SUPERSEDED (no paywall). Typecheck: 3 pre-existing Challenge errors, 0 new. No remaining RevenueCat/entitlement code references (PremiumStatCard in analytics is an unrelated UI component name).
